{
  "name": "redis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "chmod +x ./create-cluster.sh",
    "start": "./create-cluster.sh start",
    "stop": "./create-cluster.sh stop",
    "create": "./create-cluster.sh create",
    "clean": "rm -rf appendonly*/ && rm -f redisnodes* && rm -f 700* && rm -f dump-700*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.8.10",
    "ioredis": "^5.3.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
